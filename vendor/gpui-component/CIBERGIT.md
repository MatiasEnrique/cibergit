Vendored gpui-component 0.6.1 with `pub mod chart` disabled.
Upstream chart IntoPlot macros fail to compile against gpui-pre 0.3.1; the highlighter (tree-sitter) is unaffected.
Upgrade path: switch back to crates.io once on gpui-component/gpui-base ≥ 0.6.6.
